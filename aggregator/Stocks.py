class Stocks:

    def __init__(self, config)
        self.config = config


    def get_candles(ticker, _from, to, limit, multiplier, timespan, sort="asc", result_queue=None)
    '''
    ticker	- the symbol you want to use
    _from/to	- milliseconds in unix time
    limit	- the amount of candles you want to get
    multiplier	- the amount of timespan
    timespan	-
    sort	- asc/desc
    '''

        if self.config["polygon"]["api_key"]:
        

        elif self.config["alpaca"]["api_key"]
